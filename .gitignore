__pycache__/
*.db
.env
node_modules/
frontend/dist/
venv/
uploads/

# Intermediate IND-sponsor working files (raw dumps, shortlists, collapse maps).
# The app only needs app/data/ind_sponsors.json, which IS committed.
/data/
.kiro/*
!.kiro/skills/
.hermes/*
!.hermes/skills/

# Local-only infra/secrets reference (real IPs, account IDs, bucket names).
# Never commit — repo keeps only <placeholders>.
infra.local.md
